import{createApiLab,listResult}from'./api-runtime.js?v=2026083001';const input=document.querySelector('#text');createApiLab({endpoint:'sentiment',read:()=>{const text=input.value.trim();if(!text||text.length>1000)throw new Error('請輸入 1–1000 字英文文字。');return{text}},render:(data,node)=>listResult(data.map(item=>`${item.label}　${(item.score*100).toFixed(1)}%`),node),sample:value=>{input.value=value}});
