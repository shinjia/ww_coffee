import{createApiLab,textResult}from'./api-runtime.js?v=2026083001';const x=document.querySelector('#text');createApiLab({endpoint:'embedding',read:()=>{const text=x.value.trim();if(!text||text.length>1500)throw Error('請輸入 1–1500 字英文文字。');return{text}},render:(d,n)=>{const v=d.embedding||[],norm=Math.sqrt(v.reduce((s,x)=>s+x*x,0));textResult(`向量維度：${d.dimensions}\nL2 norm：${norm.toFixed(6)}\n前 16 維：\n${v.slice(0,16).map(x=>x.toFixed(6)).join(', ')}`,n)}});
