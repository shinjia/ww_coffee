# AGENTS.md

本文件為後續 AI／Codex 開發木窗咖啡網站時必須遵守的規範。

## 開始與完成流程

1. 修改前完整閱讀 `README.md`、`AGENTS.md`、`HANDOFF.md`、`CHANGELOG.md`。
2. 先盤點現有功能與檔案，只做需求範圍內的變更。
3. 完成後測試主要流程、手機版面、Console、404 與外部資源。
4. 每次修改後同步更新上述四份文件，確保內容符合實際狀態。
5. `CHANGELOG.md` 記錄對使用者、部署或後續開發有意義的變更，不記錄無關的操作細節。

## 開發規範

- 使用原生 HTML、CSS、JavaScript；無明確必要性不得增加框架或套件。
- HTML、CSS、JavaScript 分離，保持檔案結構簡單。
- 採 Responsive Web Design，避免文字、圖片、按鈕溢出或遮擋。
- 以 Chrome 為主要測試瀏覽器，兼顧 Edge、Safari 等現代瀏覽器。
- 所有使用者輸入需驗證空值、格式、長度及異常資料。
- 所有非同步操作需提供載入中、完成與錯誤狀態，並以 `try/catch/finally` 保護流程。
- 不得將密碼、API Key、Token 或敏感資訊放入前端。
- 使用外部 CDN、API、模型或套件前，記錄來源、版本、用途及失敗處理。
- 修改既有功能時避免大幅重構，優先維持相容性。
- 所有 HTML 內連結的 `.css`、`.js` URL 必須有 `?v=20260830xx`；每次該檔案更新，`xx` 加一。

## 現有設計原則

- 色彩：燕麥米、墨黑、木質棕、沉靜綠。
- 風格：安靜、溫暖、有留白的當代咖啡品牌。
- 未取得真實店址、電話、社群、菜單與售價前，不可虛構資訊。
- 品牌前台固定放在 `web/`；專案根目錄 `index.html` 只作網站與各子系統的清單入口。
- 主視覺 `web/assets/images/ww-coffee-hero.webp` 是專案本地資產，不應改為不穩定的外部圖片 URL。
- 新增可供使用者直接進入的子系統時，需同步更新根目錄清單頁，並明確標示「對外」、「內部」或「實驗」。
- 不得把專案目錄名稱寫死在站內連結；PHP 後台一律使用 `mis_base_url()`，靜態頁使用專案內相對路徑，確保整個目錄搬移後仍可使用。
- 根目錄清單固定依「網站對外」、「內部管理」、「Lab 實驗」三區呈現，新入口需放入相符區段。
- 主要標題需使用可適應視窗的字級與 `word-break: keep-all`，避免中文字詞被擠成過多行或溢出。

## MIS 開發規範

- 子系統命名為 `mis_xxx`，且必須在該目錄保留 `xxx.md`，記錄用途、角色權限、資料、流程、限制與快取版本。
- 共用設定、SQLite、驗證、安全函式與資產集中在 `mis_shared`，子系統不得複製另一套共用核心。
- 使用共用函式前必須確認函式已在 `mis_shared` 定義並由 `bootstrap.php` 載入；環境判斷可直接讀取 `mis_load_config()['environment']`。
- 後端維持 PHP 7.4 相容，不使用 PHP 8 專屬語法；資料存放使用 SQLite、JSON 或必要的文字檔，不使用 MySQL。
- 所有 SQL 必須使用 PDO prepared statements；啟用 foreign keys，寫入流程需要交易時必須使用 transaction。
- 密碼只允許以 `password_hash()` 儲存，禁止保存或記錄明文。依本教學需求，員工與會員使用「帳號加 `123`」固定密碼，但只允許在 development 環境；正式部署必須移除此規則。
- 所有狀態變更必須在後端做角色、CSRF、格式、長度與允許值驗證；不得只依賴前端限制。
- 管理與人資頁正式部署時必須限制為內網或 VPN；`mis_shared/config`、`mis_shared/storage` 及備份不得公開存取。
- 客戶留言等公開表單必須具備 CSRF、防機器人／頻率限制、個資告知、成功與錯誤狀態，不得將後端例外細節回傳給使用者。
- 管理操作、登入成功／失敗及公開資料新增應寫入 `audit_logs`，但不得記錄密碼、Session ID 或完整敏感內容。
- MIS 共用與子系統 CSS／JS 同樣使用 `?v=20260830xx`，每個更新檔案獨立遞增。

## Browser AI Lab 規範

- `lab_hugging` 使用原生 HTML、CSS、JavaScript，不建立 npm、打包或框架流程。
- Transformers.js 固定為 `3.8.1`；更換版本前須查核官方文件、模型相容性並更新來源與測試紀錄。
- 推論結果必須來自真實模型，不得寫死答案、偽造信心度或在失敗時顯示假結果。
- 模型切換後必須停用舊 Pipeline，重新載入成功前不得執行。
- 模型下載、載入、推論、完成及失敗狀態必須顯示於畫面，完整錯誤保留於 Console。
- 新增模型或 Pipeline 時，須在 `lab_hugging/hugging.md` 記錄 Model ID、Task、語言、大小、Transformers.js／WASM／WebGPU 與實測狀態。
- 每一種 Pipeline 必須使用獨立 HTML 與專用 JS；`index.html` 只作入口，不得把多種應用混在同一個實驗頁。
- 允許 `runtime.js` 共用模型載入與狀態處理，但各任務的欄位、參數與結果轉換必須留在該任務專用 JS。
- Sentence Similarity 必須以模型產生的 Embedding 計算 cosine similarity；Feature Extraction 必須呈現真實向量維度與數值，不得以固定分數或範例向量代替。

## Python Server AI Lab 規範

- `lab_hugging` Browser AI 必須完整保留；Server AI 只在獨立的 `lab_hugging_python` 開發。
- 每個任務需有獨立 HTML、JS 與 `api/tasks` Python 模組；共用 API 狀態放在 `api-runtime.js`，模型生命週期放在 `model_manager.py`。
- 模型採首次請求載入、後續常駐重用；不得每次請求重新建立 Pipeline，也不得一次無條件預載全部模型。
- Python 套件必須固定版本並使用專用虛擬環境；模型與套件來源、用途及測試狀態記錄於 `hugging_python.md`。
- API 必須在後端再次驗證型別、空值、長度與允許範圍，限制 Body、Origin，不回傳 traceback、內部路徑或敏感資訊。
- 前端必須顯示連線／模型載入等待、完成與錯誤狀態；API 回傳模型是否快取及載入、推論、總耗時。

## 商品、QR 點餐、會員與 AI 推薦規範

- 子系統命名為 `mis_shop`，階段 1 至 10 已完成；後續變更仍須維持分析指標、虛擬／非虛擬資料切換及既有整合測試行為。
- 系統定位為教學示範，範圍限商品目錄、QR Code 點餐、訂單、成品庫存、顧客會員與 AI 推薦；不建立原料／配方庫存、供應商、採購、金流、電子發票或外送串接。
- 示範商品與價格可寫入資料庫，但公開頁面及文件必須明示為課程示範資料，不得冒充正式菜單。
- 商品除名稱、分類、價格、圖片、庫存與狀態外，需保留冰熱、咖啡因、甜度、風味、口感、價格區間、飲食標籤、過敏原、使用情境與搭配資訊，以支援後續 AI 教學。
- 庫存只管理成品數量。建立訂單與扣庫存、取消訂單與回補庫存需視流程使用同一 transaction，禁止負庫存及超賣；人工調整、售出與回補都必須保留異動紀錄。
- 商品價格、可售狀態、桌號與庫存一律由後端重新驗證；不得相信 QR Code、Query String、Hidden Input、Local Storage 或前端傳入的總額。
- QR Code 只可包含公開點餐入口或不具個資及權限的桌號識別；不得包含會員 ID、Email、價格、登入狀態或管理權限。顧客確認前，AI 不得自動送出訂單。
- 訂單明細需保存下單當時的商品名稱、選項與單價快照，後續商品改名或調價不得改寫歷史訂單。
- 免登入點餐必須保留。顧客會員不可直接沿用員工 `users` 權限模型，應與 MIS 員工帳號分離，並遵守密碼雜湊、CSRF、Session、頻率限制、後端驗證與帳號停用規範。
- 個人化推薦必須先取得明確同意，只收集教學功能需要的偏好、收藏、訂單與互動；提供停止個人化、清除推薦紀錄及資料用途／保存期限說明，不預設收集電話、地址、生日或性別。
- 過敏原及飲食限制必須由結構化資料做強制排除。AI 不得猜測過敏原、價格、庫存或商品內容；資料不足時應提示詢問店員。
- 推薦依序從熱門／規則式基準、內容式推薦，再擴充至自然語言條件抽取、Embedding 語意搜尋、協同過濾與混合推薦；模型失敗時要降級至一般目錄或規則式推薦。
- 推薦結果需提供原因、不感興趣、停止推薦及偏好修改入口；推薦紀錄使用匿名或最小識別資料，並記錄方法、結果與回饋，供課程評估而非只展示輸出。
- AI 教學需涵蓋冷啟動、多樣性、熱門偏誤、新品探索、A/B Test、準確率／接受率等評估、隱私、幻覺及安全邊界。
- 分析頁只能把虛擬資料視為教學樣本，預設排除；需求預測需標明算法與限制，異常偵測只能列出候選，不得自動取消訂單或處分會員。
- 實驗後台可依指定數量、日期範圍及亂數種子整批建立員工、意見留言、會員、偏好、訂單、明細、推薦紀錄與回饋。產生資料必須符合 foreign key、價格、狀態與格式規則，不得用互不相干的隨機列假裝完整流程。
- 每次產生需有批次編號、`is_synthetic` 或等效來源標記、參數、種子、建立者、筆數及狀態；相同設定應可重現，以便比較演算法及課程成果。
- 虛擬資料產生及清除只允許 `admin` 且限明確啟用的 development／Lab 環境；production 預設拒絕。禁止使用真實個資、寄信、付款、通知或第三方副作用。
- 產生器需提供數量上限、預覽、載入／完成／錯誤狀態及稽核紀錄。預設不扣正式成品庫存；若示範扣庫存，必須明確選擇並產生一致的庫存異動。
- 清除功能只能依已解析並驗證的批次 ID 刪除該批次虛擬資料，需再次確認並遵守 foreign key 順序；不得提供清空整張正式資料表或依未驗證條件大量刪除的捷徑。
- 預定開發順序為：需求與資料設計、商品目錄、QR 點餐、成品庫存、會員、規則／內容式推薦、AI 擴充、實驗資料產生器、分析評估、整合驗收。每階段均需獨立測試並同步四份核心文件。
- `mis_shop` 可直接使用的公開、會員、管理、虛擬資料與 AI 實驗入口都必須同步列在根目錄 `index.html` 的相符區段，不能只藏在子系統頁面內。
