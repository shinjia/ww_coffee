import{createTextLab,listResult}from'./runtime.js?v=2026083001';const text=document.querySelector('#text'),labels=document.querySelector('#labels');createTextLab({task:'zero-shot-classification',model:'Xenova/mobilebert-uncased-mnli',read:()=>{const t=text.value.trim(),l=labels.value.split(',').map(x=>x.trim()).filter(Boolean);if(!t||l.length<2)throw new Error('請輸入文字及至少兩個英文標籤。');return{t,l}},infer:(p,i)=>p(i.t,i.l),render:(o,n)=>listResult(o.labels.map((l,i)=>`${i+1}. ${l}　${(o.scores[i]*100).toFixed(1)}%`),n),sample:s=>{text.value=s.text;labels.value=s.labels}});
